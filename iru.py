# Splitting a string into substrings using ',' as the separator
string_to_split = "apple,banana,cherry"
result = string_to_split.split(',')

print(result)  # Output: ['apple', 'banana', 'cherry']
